<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.daivansh.androidmanagementmdm.ui.activity.DeviceActivity">
    <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="20dp"
            android:text="List Devices"
            android:id="@+id/mBtnListDevices"
            android:layout_alignParentTop="true"
            android:layout_margin="15dp"/>
    <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="No Device Found"
            android:id="@+id/mTxtNoDeviceFound"
            android:textSize="25dp"
            android:layout_below="@+id/mBtnListDevices"
            android:visibility="gone"
            android:gravity="center"/>

    <android.support.v7.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/mRecyclerView"
            android:layout_below="@+id/mBtnListDevices"
            android:layout_centerVertical="true"
            android:visibility="gone"/>
</RelativeLayout>